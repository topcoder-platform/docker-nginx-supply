server {
    listen 8000;
	server_name url3623.{{ENV_TLD}};
	include includes/forceSSL.nginx.conf;
	root /data/nginx/apache_docs/tcdocs;

    location = / {
        include includes/url3623.proxy.nginx.conf;
    }

    location / {
        include includes/url3623.proxy.nginx.conf;
    }
}
